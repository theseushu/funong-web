import { selector as moduleSelector, actions } from './ducks';

export const selector = moduleSelector;

export const searchDistrict = actions.searchDistrict;
export const searchSubdistrict = actions.searchSubdistrict;
